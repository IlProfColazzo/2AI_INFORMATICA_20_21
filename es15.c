//Gaetani Roberto 2 AI
/*Determinare e visualizzare la media di tre voti (attenzione al tipo di variabili che si utilizzano).*/
#include <stdio.h>
#include <stdlib.h>

int main()
{    /**Determinare e visualizzare la media di tre voti (attenzione al tipo di variabili che si utilizzano).*/
    float voto1=7.5;
    float voto2=7.8;
    float voto3=8.5;
    float media=(voto1+voto2+voto3)/3;

    printf ("Primo voto:%f\n",voto1);
    printf ("Secondo voto:%f\n",voto2);
    printf ("Terzo voto:%f\n",voto3);
    printf ("Media dei voti:%f",media);
    return 0;
}
